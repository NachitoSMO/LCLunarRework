<Project>
    <!-- Custom Variables -->
    <PropertyGroup>
        <PluginDirectory>C:\Users\ninte\AppData\Roaming\com.kesomannen.gale\lethal-company\profiles\mod\BepInEx\plugins\</PluginDirectory>
    </PropertyGroup>

    <!-- Copy to Plugin Directory for Quicker Testing -->
    <Target Name="CopyFiles" AfterTargets="PostBuildEvent">
        <Copy SourceFiles="$(TargetPath)" DestinationFolder="$(PluginDirectory)"/>
    </Target>
</Project>
